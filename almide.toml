[package]
name = "fizz_x_mention_source"
version = "0.1.0"
description = "Fizz comment source — X (Twitter) mentions via XAPI proxy → normalized Comment NDJSON"

[dependencies]
fizz_protocol = { git = "https://github.com/aiviecast/fizz-protocol", tag = "v0.2.2" }
